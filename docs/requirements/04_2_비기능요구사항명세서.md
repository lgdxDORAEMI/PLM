# 비기능 요구사항 명세서: ThinQ Pregnancy Life Mode

- 작성 기반: `PRD.md`, `MVP.md`, `유스케이스다이어그램.md`, `유스케이스명세서.md`, `기능요구사항명세서.md`
- 비기능 ID 규칙: `NFR-XXX` (일련번호 3자리)
- 구분: 성능 / 보안 / 개인정보 / 안정성 / 사용성 / 접근성

---

## 1. 성능

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-001 | 성능 | AI 엔진의 하루 루틴 생성 응답은 사용자가 체감 가능한 시간 내에 완료되어야 한다. | API 응답시간 p50 ≤ 5초, p95 ≤ 10초 (부하테스트로 검증) |
| NFR-002 | 성능 | 주요 화면(컨디션 체크, 루틴 홈, 캘린더)은 빠르게 로딩되어야 한다. | 화면별 TTI(Time To Interactive) ≤ 2초 (4G/LTE 환경 기준) |
| NFR-003 | 성능 | 서비스는 동시 다발적 사용자 접속을 안정적으로 처리해야 한다. | 동시 접속 1,000명 부하테스트에서 오류율 < 1%, 평균 응답지연 ≤ 3초 |
| NFR-004 | 성능 | AI 챗봇 대화 응답은 실시간 대화로 인지될 수 있는 수준이어야 한다. | 챗봇 응답시간 p95 ≤ 8초 |

---

## 2. 보안

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-005 | 보안 | 사용자 인증은 표준 인증 방식(OAuth 2.0/JWT)을 사용하고 세션 토큰은 일정 시간 후 만료되어야 한다. | 액세스 토큰 만료 시간 ≤ 24시간, 만료 후 재인증 요구율 100% |
| NFR-006 | 보안 | 가전 제어 API 호출은 반드시 사용자 인증과 기기 소유권(가족 계정 권한) 검증을 거쳐야 한다. | 무권한 제어 시도 차단율 100% (침투테스트/모의해킹으로 검증) |
| NFR-007 | 보안 | 서버-클라이언트, 서버-외부 API(AI 엔진, ThinQ 가전, 홈카메라) 간 모든 통신은 암호화되어야 한다. | 전 구간 TLS 1.2 이상 적용, 평문 통신 구간 0건 |
| NFR-008 | 보안 | 저장 데이터 중 민감정보(건강 수치, 영상 파생 데이터)는 저장 시 암호화되어야 한다. | AES-256 이상 암호화 적용률 100% |
| NFR-009 | 보안 | AI 엔진 등 외부 API 연동 시 인증키/토큰은 안전하게 관리되어야 한다. | 시크릿 값 코드/저장소 노출 0건 (정적 분석 도구 검사) |

---

## 3. 개인정보

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-010 | 개인정보 | 임신·건강 정보(혈당, 체중, 통증 점수, 컨디션 기록 등)는 민감정보로 분류해 일반 데이터와 분리 관리되어야 한다. | 민감정보 필드 암호화·접근제어 적용률 100% |
| NFR-011 | 개인정보 | 홈카메라 원본 영상은 장기 보관하지 않고, 활동 로그 등 파생 데이터만 유지해야 한다. | 원본 영상 보관 기간 ≤ 7일, 초과 보관 0건 |
| NFR-012 | 개인정보 | 사용자는 홈카메라 기반 모션 모니터링 동의를 언제든 철회할 수 있어야 하며, 철회 시 즉시 데이터 수집이 중단되어야 한다. | 동의 철회 요청 후 데이터 수집 중단까지 소요시간 ≤ 1분 |
| NFR-013 | 개인정보 | 남편 계정과 공유되는 데이터 범위는 아내가 명시적으로 동의한 항목으로 제한되어야 한다. | QA 체크리스트 기준 미동의 항목 노출 0건 |
| NFR-014 | 개인정보 | AI 엔진(외부 API)에 전달되는 사용자 데이터는 서비스 목적(루틴 생성)에 필요한 최소 항목으로 제한되어야 한다. | 외부 전송 데이터 항목 대비 실제 사용 항목 비율 100% (불필요 항목 전송 0건) |

---

## 4. 안정성

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-015 | 안정성 | 서비스는 안정적으로 상시 이용 가능해야 한다. | 월간 서비스 가용성(Uptime) ≥ 99.5% (월 다운타임 ≤ 3.6시간) |
| NFR-016 | 안정성 | AI 엔진 응답 실패 시에도 사용자에게 빈 화면이 노출되지 않아야 한다(W-ROUTINE-003 폴백 연계). | 폴백 로직 작동 성공률 ≥ 99.9%, 빈 화면 노출률 < 0.1% |
| NFR-017 | 안정성 | 외부 시스템(ThinQ 가전, 홈카메라) 장애가 발생해도 나머지 핵심 기능은 정상 동작해야 한다. | 장애 격리 테스트에서 타 기능 정상 동작률 100% |
| NFR-018 | 안정성 | 사용자 데이터(프로필, 컨디션, 루틴 기록)는 주기적으로 백업되어야 한다. | 일 1회 이상 자동 백업 성공률 100%, 분기 1회 이상 복구 테스트 수행 |

---

## 5. 사용성

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-019 | 사용성 | 임산부의 신체적 부담을 고려해 컨디션 체크는 짧은 시간 내 완료 가능해야 한다. | 사용성 테스트 평균 완료 시간 ≤ 60초 |
| NFR-020 | 사용성 | 주요 기능(컨디션 체크, 루틴 확인, 가사 요청)은 최소한의 탐색으로 접근 가능해야 한다. | 홈 화면 기준 최대 탐색 깊이 ≤ 3 tap |
| NFR-021 | 사용성 | AI 챗봇/가이드 문구는 전문용어를 최소화한 평이한 언어로 제공되어야 한다. | 사용자 이해도 설문 평균 4.0점 이상 (5점 척도) |

---

## 6. 접근성

| 비기능 ID | 구분 | 요구사항 | 측정 기준 |
|---|---|---|---|
| NFR-022 | 접근성 | 주요 화면의 텍스트-배경 대비는 접근성 가이드라인을 충족해야 한다. | WCAG 2.1 AA 기준 명도대비 4.5:1 이상, 검사 도구 기준 100% 충족 |
| NFR-023 | 접근성 | 스크린 리더(VoiceOver/TalkBack) 사용자도 핵심 플로우를 이용할 수 있어야 한다. | 핵심 유스케이스(UC1~UC10) 스크린리더 탐색 가능률 100% |
| NFR-024 | 접근성 | 임산부의 손목/손가락 부담을 고려해 주요 버튼의 터치 영역을 충분히 확보해야 한다. | 주요 버튼 터치 영역 ≥ 44×44pt, 검수 충족률 100% |
| NFR-025 | 접근성 | 사용자가 폰트 크기를 조절해도 화면 레이아웃이 깨지지 않아야 한다. | 시스템 폰트 확대(최대 200%) 시 레이아웃 깨짐/텍스트 잘림 0건 |

---

## 7. 구분별 요구사항 수 요약

| 구분 | 항목 수 |
|---|---|
| 성능 | 4 |
| 보안 | 5 |
| 개인정보 | 5 |
| 안정성 | 4 |
| 사용성 | 3 |
| 접근성 | 4 |
| **합계** | **25** |
