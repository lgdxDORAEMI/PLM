# UI-004 Today Condition Check

## Goal

입덧, 허리/골반 통증, 피로감, 수면 상태 등 당일 컨디션을 60초 안에 입력·수정하고 저장 결과를 명확히 표현한다.

## Related Requirement

- `W-COND-001`, `W-COND-002`
- `UC2. 오늘의 컨디션 기록`

## Related Screen

- Screen ID: `SCR-W-02`
- Screen Name: 오늘의 컨디션

## Source Documents

- `DESIGN.md` §9.9, §11.4, §12~13
- `docs/requirements/04_1_기능요구사항명세서.md`
- `docs/requirements/03_유스케이스명세서.md` UC2
- `docs/서비스흐름도/02_AI하루루틴생성.mmd`
- `docs/development/03_design_system.md`
- `docs/development/04_component_system.md`
- `docs/development/05_ui_implementation_plan.md` SCR-W-02
- `docs/development/ROUTE_MAP.md`

## Entry

UI-006 Home의 미입력 CTA 또는 컨디션 수정 Action.

## Route

TBD. 후보 `/wife/home/condition`.

## Exit

최초 당일 입력은 UI-005, 수정은 UI-006 Home으로 복귀한다.

## Back Behavior

미저장 변경이 있으면 이탈 확인 후 Home으로 돌아간다.

## Required Components

### Reuse

- `AppButton`, `AppSelectionCard`, `AppProgressMetric`, `AppBanner`
- `ResponsivePageContent`, `AppTopBar`, `ConditionSummaryBanner`, `LoadingState`, `ErrorState`

### New

- `ConditionSelector`, `BodyPainSelector`
- Condition Model/State, `ConditionService`, `MockConditionService`, Controller, Fixture

## UI Structure

```text
Page
├─ AppTopBar
└─ ResponsivePageContent(form)
   ├─ title / date context
   ├─ nausea selector
   ├─ body pain selector
   ├─ fatigue metric
   ├─ sleep condition selector
   ├─ validation/error
   └─ save CTA
```

## UI State

- Loading existing value
- Default / Selected / Editing
- Validation Error
- Submitting / Disabled
- Save Error
- Success

## Interaction

- Tap/Keyboard: 선택, 단계 이동, 저장
- Input: 문서에 정의된 보조 입력만
- Hover/Focus: Card/Metric/CTA
- Scroll: 200% Text와 짧은 Viewport

## Data

날짜, 입덧, 부위별 통증/강도, 피로, 수면 상태와 수정 여부를 Frontend Model로 정의한다.

Data Source: `MockConditionService`

Future: `ApiConditionService`로 교체 예정. Partner Report Trigger는 Service 결과로만 표현하고 Backend/알림을 구현하지 않는다. Mock 값은 Fixture에 둔다.

## Responsive

- Mobile-width Web: 20px, 단일 Column
- Tablet-width Web: 최대 560px, 짧은 선택지만 조건부 2열
- Desktop: 중앙 최대 560px

## Accessibility

- Focus: 질문 순서와 저장 CTA
- Keyboard: Selection/Metric을 Arrow 또는 Tab+Space로 조작
- Semantics: 강도 숫자와 `좋아요/보통/심해요` Text 병기
- 오류는 Field와 연결

## Files to Create

- `frontend/lib/features/condition/models/condition.dart`
- `frontend/lib/features/condition/services/condition_service.dart`
- `frontend/lib/features/condition/services/mock_condition_service.dart`
- `frontend/lib/features/condition/state/condition_controller.dart`
- `frontend/lib/features/condition/pages/condition_page.dart`
- `frontend/lib/features/condition/widgets/condition_selector.dart`
- `frontend/lib/features/condition/widgets/body_pain_selector.dart`
- Fixture와 Test

## Files to Modify

- 중앙 Route/DI는 Integration Owner가 연결

## Do Not Modify

- Backend의 기존 Profile/API
- Theme/Router/`app.dart`/다른 Feature 직접 수정

## Dependency

- UI-001의 Profile/임신 주차 Mock Context
- Form Foundation과 Route Contract

## Conflict Risk

낮음~중간. UI-005와 `features/condition`을 공유하므로 Model/Service 파일 Owner를 먼저 나눈다.

## Definition of Done

- DESIGN.md와 60초 핵심 흐름 일치
- 입력/수정 Route와 Back 정상
- Mock 저장/재조회 및 Error 정상
- 반응형, 200% Text, Hover/Focus/Selected/Disabled 정상
- Partner Trigger 실패가 저장 성공을 뒤집지 않는 Mock Scenario
- Widget/Route Test, `flutter analyze`, 관련 `flutter test` 통과

## Handoff Output

```text
Status:
Implemented:
Route:
Reused:
Created:
Modified:
Mock:
Known Issues:
Next Dependency:
```
