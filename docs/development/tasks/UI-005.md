# UI-005 Planned Activity Selection

## Goal

오늘 예정된 가사 활동을 복수 선택하고 Mock Routine 생성 요청에 전달할 수 있게 한다.

## Related Requirement

- `W-ACT-001`
- `UC3. AI 하루 루틴 확인`

## Related Screen

- 대표 요구사항 ID: `W-ACT-001`
- Screen Name: 오늘 예정 활동

## Source Documents

- `DESIGN.md` §10.5, §12~13
- `docs/requirements/04_1_기능요구사항명세서.md`
- `docs/requirements/03_유스케이스명세서.md` UC3
- `docs/서비스흐름도/02_AI하루루틴생성.mmd`
- `docs/development/04_component_system.md`
- `docs/development/05_ui_implementation_plan.md` W-ACT-001
- `docs/development/ROUTE_MAP.md`

## Entry

UI-004의 최초 당일 컨디션 저장 완료.

## Route

`/wife/home/activity`을 사용한다.

## Exit

선택 저장 후 UI-006 Home의 Routine 생성 상태로 이동한다.

## Back Behavior

미저장 선택이 있으면 확인 후 UI-004 또는 Home으로 복귀한다. 정확한 진입 Stack을 따른다.

## Required Components

### Reuse

- `AppButton`, `AppSelectionCard`, `AppBanner`
- `ResponsivePageContent`, `AppTopBar`, `LoadingState`, `ErrorState`
- UI-004 Condition Feature의 당일 Context

### New

- `PlannedActivitySelector`
- Planned Activity Model/State와 Mock Fixture

## UI Structure

```text
Page
├─ AppTopBar
└─ ResponsivePageContent(form)
   ├─ question / supporting text
   ├─ multi-select activity grid
   ├─ selected summary
   ├─ submit error
   └─ create routine CTA
```

## UI State

- Default
- Selected
- Disabled
- Submitting
- Error

## Interaction

- Tap/Keyboard: 복수 선택/해제, 제출
- Hover/Focus: Selection Card와 CTA
- Transition: 선택 상태와 Home 생성 상태 전환

## Data

문서에 정의된 예정 활동 ID, Label, 선택 여부와 당일 Context 참조만 사용한다.

Data Source: `MockConditionService` 또는 UI-004에서 합의된 Condition Feature Service

Future: Routine ApiService 입력으로 Mapping 예정. API Payload를 UI Model로 확정하지 않고 Mock Fixture를 Widget에 넣지 않는다.

## Responsive

- Mobile-width Web: 단일 Column
- Tablet-width Web: 가용 폭과 Text Scale 허용 시 2열
- Desktop: 최대 560px, 고정 Card 높이 금지

## Accessibility

- Focus: 설명 → 선택지 → CTA
- Keyboard: Space/Enter 선택, 명확한 Focus Ring
- Semantics: 다중 선택과 현재 선택 상태, 선택 개수

## Files to Create

- `frontend/lib/features/condition/models/planned_activity.dart`
- `frontend/lib/features/condition/state/planned_activity_controller.dart`
- `frontend/lib/features/condition/pages/planned_activity_page.dart`
- `frontend/lib/features/condition/widgets/planned_activity_selector.dart`
- Fixture와 Test

## Files to Modify

- UI-004 Service Contract는 합의된 범위만 수정
- 중앙 Route/DI 연결은 Integration Owner가 수행

## Do Not Modify

- Routine Backend/LLM/API
- 중앙 Theme/Router/`app.dart` 직접 수정
- 다른 Feature 내부 코드

## Dependency

- UI-004
- `AppSelectionCard`
- UI-006의 Routine 생성 Navigation Intent 계약

## Conflict Risk

중간. UI-004와 같은 Feature 폴더를 사용한다. 공통 Condition Model과 Service 수정은 한 Commit에서 상호 리뷰한다.

## Definition of Done

- DESIGN.md 일치
- 복수 선택, 저장, Route/Back 정상
- Mock Data와 제출 오류 정상
- Mobile/Tablet/Desktop, 200% Text, Hover/Focus/Selected/Disabled 정상
- Widget/Route Test, `flutter analyze`, 관련 `flutter test` 통과

## Handoff Output

```text
Status:
Implemented:
Route:
Reused:
Created:
Modified:
Mock:
Known Issues:
Next Dependency:
```
