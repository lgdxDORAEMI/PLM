# 프로젝트 작업 지침

## Frontend 작업

- Frontend UI/UX 작업을 시작하기 전에 `docs/development/frontend_workflow.md`를 반드시 확인한다.
- 구현할 화면의 구조와 콘텐츠는 `docs/screens/**` 이미지를 참고하되, 시각 디자인과 UI 규칙은 `DESIGN.md`를 최우선 기준으로 적용한다. 두 자료가 충돌하면 `DESIGN.md`를 따른다.
- STEP 1~8은 사용자가 시작을 명시적으로 요청한 단계만 독립적으로 수행하며, 이전 단계 확인 전 다음 단계의 대규모 작업을 시작하지 않는다.
- 기획 문서가 업데이트되면 이전 분석을 재사용하지 않고 최신 Source of Truth를 처음부터 다시 확인한다.

## 커밋 메시지

- 이 저장소의 커밋 메시지는 항상 한글로 작성한다.
- 커밋 제목과 본문 모두 한글로 작성한다. 코드 식별자, 파일명, 라이브러리명 등 기술 명칭은 원문을 유지할 수 있다.
- 예: `초기 프로젝트 구조 및 개발 환경 설정 추가`
