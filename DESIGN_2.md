# PLM Product Design System

> PLM WebApp(Flutter)의 시각·상호작용 규칙을 정의한다. 이 문서는 **2026-09-23 기준으로 실제 구현된 화면**을 기준으로 다시 작성했다.
> 구현과 문서가 어긋나면 먼저 어느 쪽이 맞는지 팀에서 정하고, 문서를 고친 뒤 코드를 맞춘다.

## 0. DESIGN.md와 충돌하는 항목 (팀 결정 필요)

이 문서는 2026-09-23 구현을 기준으로 썼고, 기존 `DESIGN.md`와 아래 5가지가 정면으로 충돌한다. **코드는 현재 이 문서(DESIGN_2)대로 동작한다.**

| # | 항목 | DESIGN.md | DESIGN_2.md (현재 코드) | 판단 |
|---|---|---|---|---|
| 1 | 기본 서체 | Pretendard Variable + Noto Sans KR (156~163행) | LG EI Headline TTF | 결정 필요 |
| 2 | LG 브랜드 자산 | "LG 공식 색상·토큰·gradient 사용 금지", "특정 기업 디자인 시스템을 복제하지 않는다" (15, 85~92행) | LG 저작권 폰트를 앱 기본 서체로 사용 | **가장 민감. 라이선스 확인 필요** |
| 3 | 카테고리 색 사용 범위 | "작은 아이콘 배경·태그·범례에만. 큰 카드 배경을 카테고리 색으로 채우지 않는다" (134행) | 인사 카드·상단 카드·배지를 카테고리 색 40%로 채움 | 결정 필요 |
| 4 | 카테고리 배경 값 | 연한 고정색(`#F0F3EA` 등) | 카테고리 색 40% 알파 | 결정 필요 |
| 5 | 본문 최소 크기 | "Mobile에서도 14px 미만 금지" (178행) | 보조 정보에 12px 사용 | 결정 필요 |

### 12px을 쓰는 위치 (충돌 #5)

| 화면 / 컴포넌트 | 대상 텍스트 |
|---|---|
| 홈 — 요약 행 | 컨디션·할 일 요약 값, "수정" 링크 |
| 남편 초대 | 혜택 카드 설명 |
| 식사 — 끼니 카드 | "지금" 배지 |
| 식사 — 추천 이유 | 근거 문장 |
| 가사 — 섹션 카운트 | "2개 · 가볍게", "2개 할일", "추천 2개" |
| 가사 — 할 일 카드 | 카드 설명·가전 이름 |
| 수면 — 환경 카드 | 항목 라벨(조명·온도·습도·소리·공기청정기) |
| 캘린더 — 범례 | "컨디션 지수" (13px) |
| Daily 리포트 — 상단 카드 | "실행 기록은 내일 추천 정확도에 반영됩니다" |
| Daily 리포트 — 루틴 카드 | 분야 배지, "완료 / 오늘은 건너뜀" |

> 2026-09-23 조정: 공용 HeroCard 설명(13 → 14)과 홈 "오늘 컨디션을 반영한 맞춤 가이드를 확인해보세요."(12 → 14)는 14px로 올려 목록에서 빠졌다.
> 남은 항목은 모두 보조 정보(설명·상태·카운트)이고, 핵심 상태 문구와 본문은 14px 이상을 유지한다. DESIGN.md 규칙을 그대로 지키려면 위 항목을 14px로 올려야 하고, 시안과 카드 높이·간격이 달라진다.

## 1. Purpose

- 아내에게는 오늘의 상태와 해야 할 일을 빠르게 이해하고 기록할 수 있는 경험을 준다.
- 남편에게는 요청·리포트·일정이 명확히 구분되어 행동으로 이어지는 경험을 준다.
- AI 결과는 근거와 상태를 함께 보여주는 보조 수단으로 표현한다.
- Mobile(393 기준) 우선, Tablet·Desktop에서는 정보 배치를 다시 구성한다.
- 같은 토큰과 컴포넌트 계약을 Flutter 구현에서 그대로 재사용한다.

## 2. Source of Truth

판단 순서는 다음과 같다.

1. `docs/requirements/04_1_기능요구사항명세서.md`
2. `docs/requirements/03_유스케이스명세서.md`
3. `docs/requirements/01_MVP.md`, `01_PRD.md`
4. `docs/development/ROUTE_MAP.md` (Route)
5. `docs/SCREEN_IMPLEMENTATION_MAP.md` (Screen ID)
6. 이 문서 (색·타이포·간격·컴포넌트 외형·반응형)
7. Figma 파일 `IpRD5Y8GgN5HVgQAEa8eO4` 페이지 "UI 작업" (시안 원본)

원칙:

- 기능·상태·권한은 requirements가 우선한다.
- 화면 외형은 이 문서가 최종 기준이며, Figma 시안은 이 문서를 갱신하는 입력이다.
- **Figma 시안이 요구사항의 기능을 없애는 것처럼 보이면 먼저 확인한다.** (예: 홈 "이번 주에 알아두세요"는 Backend `home.week_notes`를 쓰므로 시안에 없다고 삭제하지 않는다.)
- Backend가 주지 않는 값을 화면에 넣어야 하면 FE에서 지어내지 않고 **BE 요청 목록**으로 남긴다.

## 3. Design Principles

1. **Calm clarity** — 한 화면의 핵심 질문과 Primary CTA는 하나로 제한한다.
2. **Warm, not decorative** — 따뜻함은 크림 표면·낮은 채도 포인트로 표현한다. 전체를 pink/pastel로 만들지 않는다.
3. **Information before ornament** — 상태·추천·요청의 의미가 장식보다 먼저 읽혀야 한다.
4. **One decision at a time** — 입력 화면은 한 단계의 판단에 집중한다.
5. **Explainable AI** — 추천 내용, 이유, 대안 경로, 생성 상태를 함께 보여준다.
6. **Actor-aware** — 아내/남편 화면은 색·구조·정보량을 구분한다.
7. **카테고리 색으로 맥락을 표시한다** — 식사·가사·건강·수면 화면은 해당 카테고리 색을 채움·테두리·배지에 일관되게 쓴다.

## 4. Typography

```text
Font family: "LG EI Headline TTF" (frontend/assets/fonts)
Fallback: "Pretendard Variable", "Noto Sans KR", "Inter", system-ui
```

굵기 매핑(`pubspec.yaml`):

| 지정 weight | 실제 파일 |
|---|---|
| 400 | LGEIHeadlineTTF-Regular |
| 500 | LGEIHeadlineTTF-Semibold |
| 600 | LGEIHeadlineTTF-Semibold |
| 700 | LGEIHeadlineTTF-Bold |

> LG EI Headline은 LG Electronics 저작권 폰트다. 저장소 반영 전 사용 범위를 확인한다.

### 4.1 Type scale (`AppTypography`)

| Style | Size / Line height | Weight | Usage |
|---|---|---:|---|
| display | 36 / 46 | 700 | Desktop 핵심 수치 |
| heading1 | 28 / 38 | 700 | 화면 제목 |
| heading2 | 22 / 31 | 700 | 섹션 제목, 카드 대표 문구 |
| heading3 | 18 / 27 | 600 | 카드·패널 제목 |
| bodyLarge | 17 / 27 | 500 | 추천 설명 |
| body | 15 / 24 | 400 | 기본 본문 |
| label | 14 / 20 | 600 | 입력 label, button |
| caption | 12 / 18 | 500 | 날짜·보조 상태 |

### 4.2 화면에서 쓰는 조합

| 용도 | 값 |
|---|---|
| 상단 바 제목 | 18 / 700 |
| Hero 카드 제목 | 24~26 / 700 (홈·초대 24, 리포트·수면 22) |
| Hero 카드 설명 | 14~15 / 400 |
| 섹션 제목 | 18 / 600~700 또는 22 / 700 (가이드 화면) |
| 섹션 설명 | 12~15 / 400 |
| 카드 제목 | 16~18 / 500~700 |
| 카드 설명 | 12~15 / 400~500 |
| 목록 요약 값 | 12 / 400 |

규칙:

- 12px은 보조 정보(설명·상태·날짜)에만 쓴다. 핵심 상태 문구는 14px 이상으로 둔다.
- 한 화면 안에서 같은 역할의 텍스트는 같은 크기·굵기를 쓴다.
- 긴 문장은 본문 폭 70자 안팎으로 제한하고, 목록 요약만 한 줄 말줄임을 쓴다.

## 5. Color

### 5.1 Product palette (`AppColors`)

```text
canvas        #F6F4EF    surface       #FFFEFB
surfaceSubtle #F0EEE8    surfaceRaised #FFFFFF
textPrimary   #202624    textSecondary #58615D
textTertiary  #68716C    textInverse   #FFFFFF    textDisabled #8A908D
borderSubtle  #DEDCD4    borderStrong  #C7C9C1
primary 50 #EDF5F2 · 100 #E4F0EC · 200 #B7D4CB · 300 #8FBBAE · 400 #679F90
        500 #4E8779 · 600 #3D7165 · 700 #315C53 · 800 #284A43 · 900 #213D38
accentWarm #8A684F / bg #F4EEE8      focusRing #5E9185      scrim #202624 60%
```

### 5.2 Semantic

| 의미 | 전경 | 배경 |
|---|---|---|
| success | #39715C | #EAF4EF |
| warning | #8B6728 | #F7F1E4 |
| danger | #A54742 | #F8ECEA |
| info | #496F8A | #EAF1F5 |
| disabled | #8A908D | #ECECE8 |

### 5.3 Category

| 카테고리 | 색 | 배경 사용 |
|---|---|---|
| 식사 meal | #657A50 | 카테고리 색 **40% 채움** |
| 가사 household | #8A684F | 카테고리 색 40% 채움 |
| 건강 health | #526F88 | 카테고리 색 40% 채움 |
| 수면 sleep | #696787 | 카테고리 색 40% 채움 |
| 파트너 partner | #596D76 | #EDF1F2 |
| AI | primary600 | primary100 |

> 2026-09 시안부터 카테고리 배경은 기존 연한 고정색 대신 **카테고리 색 40% 알파**를 기본으로 쓴다. 토큰 `category*Background`는 남아 있으나 신규 화면에서는 `withValues(alpha: 0.4)`를 쓴다.

### 5.4 Color rules

- 상태를 색으로만 구분하지 않는다. 제목·아이콘·문구를 함께 쓴다.
- 채도가 높은 채움(primary600, 카테고리 100%) 위의 글자는 `textInverse`를 쓴다.
- 한 화면에서 채움 강조는 Hero 카드와 Primary CTA 정도로 제한한다.
- 반투명 배경 위에 그림자를 얹지 않는다. Flutter에서 그림자가 비쳐 회색 띠가 생기므로 **합성한 불투명색**을 쓴다(예: 홈 루틴 패널 `#F9FAF7`).

## 6. Spacing · Grid · Responsive

`AppSpacing`: xs 4 · sm 8 · md 12 · lg 16 · xl 24 · xxl 32 · xxxl 40 · huge 48 · massive 64
페이지 좌우 여백: mobile 20 · tablet 32 · desktop 48 · wide 64

| 구간 | Breakpoint | 열 | 기본 배치 |
|---|---|---:|---|
| Mobile | < 600 | 4 | 단일 컬럼 스크롤 |
| Tablet | 600–1023 | 8 | 카드 2열, 목록은 단일 |
| Desktop | 1024–1439 | 12 | `ResponsiveSplitView` 7:5 / 8:4 |
| Wide | ≥ 1440 | 12 | 최대 폭 1200~1440 제한 |

섹션 간격(Mobile 기준):

- 화면 상단 Hero → 첫 섹션: 24~27
- 섹션 ↔ 섹션: 32
- 섹션 제목 → 콘텐츠: 8~16
- 카드 ↔ 카드: 6~12
- 마지막 섹션 → 하단 CTA: 24

## 7. Shape · Elevation · Icon

| 요소 | Radius |
|---|---|
| Hero·요약 카드 | 20 (홈 주차 카드만 15) |
| 카드·배너·패널 | 16 |
| 버튼·입력 | 12 |
| 작은 배지·아이콘 박스 | 5~8 |
| Pill | 999 |

`AppElevation`: level1 `0 1 3 / 8%` · level2 `0 8 24 / 10%` · level3 `0 18 48 / 16%`

- 카드 기본은 그림자 없이 `borderSubtle` 1px.
- 떠 있는 느낌이 필요한 Hero·패널에만 level1~2를 쓴다.
- 아이콘은 Material Symbols(Rounded) 계열 outline을 기본으로 하고, 강조 시 filled를 쓴다. 크기 20·24·30.

## 8. Components

### 8.1 HeroCard (`design_system/components/hero_card.dart`)

화면 첫 맥락을 전달하는 공용 카드. 홈 주차 카드와 남편 초대 상단이 쓴다.

- primary600 채움, radius 15, padding 20×25, `AppElevation.level3`
- 제목 24 / 700 / `textInverse`, 설명 14 / 400 / `primary50`, 간격 2

### 8.2 TopAppBar

- 제목 18 / 700, 가운데 정렬, 배경 `surface`, elevation 0
- 좌측 back(48×48), 우측 메뉴(48×48)

### 8.3 Button (`AppButton`)

- 높이 48(최소 44), radius 12, 라벨 14 / 600
- variant: primary(primary600 채움) · secondary(테두리 `borderStrong`) · tertiary · destructive
- `color` 옵션으로 채움색을 바꿀 수 있다(가사 가이드 공유 버튼 = `accentWarm`).
- 한 화면에 Primary는 하나. 두 개를 나란히 둘 때는 같은 폭으로 배치한다.

### 8.4 카드 계열

| 카드 | 규격 |
|---|---|
| 기본 `AppCard` | surface + borderSubtle 1px, radius 16, padding 16 |
| 루틴 가이드 카드(홈) | padding 12·16·16·16, 아이콘 박스 52(카테고리 40%), 아이콘 30, 제목 18/700, 설명 14/1.71 |
| 끼니 카드 | padding 16, 아이콘 박스 52(radius 8), 제목 18/600, 요약 15 |
| 가사 할 일 카드 | padding 16, 아이콘 24, 제목 16/500, 설명 12/500, 우측 체크박스 또는 실행 버튼 |
| 수면 환경 카드 | 2열, padding 16×11, 아이콘 원 40, 라벨 12/500, 값 14/500 |
| 리포트 루틴 카드 | padding 10, 분야 배지 42×42(radius 5, 카테고리 40%), 제목 14/500, 상태 12/500 |

### 8.5 배지·요약 행

- 카테고리 배지: 카테고리 색 40% 배경 + 카테고리 색 글자 12 / 500
- 상태 배지(공유됨·확인됨·완료됨·남편): semantic 배경 + semantic 글자
- 홈 요약 행: `disabledBackground`, radius 10, 최소 높이 40, 체크 아이콘 24 + 라벨 16/600 + 값 12 + "수정" 링크 12(primary600)

### 8.6 상태 표시

- Loading: 스켈레톤 또는 `AppLoadingState`, 문구로 무엇을 준비 중인지 알린다.
- Empty: 제목 + 다음 행동 안내, 필요하면 CTA.
- Error: 원인 요약 + 재시도 버튼.
- 연동 전: `IntegrationRequiredState`(warning 톤 배너)로 데이터 없음을 알린다.

## 9. Navigation

- 아내: 하단 탭 4개(홈·실시간·챗봇·캘린더), 높이 72, 선택 탭은 primary100 pill + primary700.
- 남편: 캘린더가 홈이며 상단 알림 액션을 갖는다. 같은 캘린더 화면 코드를 공유하므로 **캘린더 변경은 두 역할 모두에 영향을 준다.**
- 가이드 상세는 뒤로가기로 이전 화면, 스택이 없으면 홈으로 복귀한다.

## 10. Screen specs (2026-09-23 구현 기준)

### 10.1 아내 홈 `W-HOME-001`

공통: 상단 주차 HeroCard → 27 → 컨디션 섹션.

**컨디션 입력 전**
- 섹션 제목 "오늘 컨디션" 18/700, 설명 "10초면 끝나요 · 입력 후 4가지 가이드를 준비해요." 14(tertiary)
- 설명 → 버튼 9, CTA "오늘의 컨디션 체크하러 가기"
- 버튼 → "이번 주에 알아두세요" 48

**컨디션 입력 후**
- 섹션 제목 "컨디션 체크" 18/600 + 저장 안내 14
- 요약 행 2개(컨디션 / 할 일), 각 행에 "수정" 링크
- "오늘 하루 루틴" 18/600 + 설명 14 → 루틴 패널(`#F9FAF7`, radius 10, level1, 카드 4장)
- "루틴 진행도" 18/600 + `N / 4 완료`(분야 단위) + 진행 바 8
- Primary CTA "오늘의 일정 마치기"
- "이번 주에 알아두세요": 제목 18/600 + 설명 14, 카드는 주차 안내 2줄 + 주의 문구(Backend `home.week_notes`, `home.caution`)

### 10.2 식사 가이드

**끼니 선택**
- 인사 카드: 식사 색 40% 채움, radius 20, padding 22×24, 제목 22/700(primary900), 설명 15, 해 아이콘 55
- "어떤 끼니를 볼까요?" 18/700 + 안내 15
- 끼니 카드 4장: 아이콘 박스 배경이 아침→밤으로 20 → 40 → 75 → 100%로 진해진다. 현재 끼니는 primary400 테두리 2px + "지금" 배지
- 현재 끼니 판단은 Backend가 주지 않으므로 BE 요청 항목이다.

**끼니 상세(아침·점심·저녁·밤 공통)**
- 끼니 이름 22/700
- 추천 이유 박스: 채움 없음 + 식사 색 테두리, 제목 18/700(primary700), 본문 15, 구분선 후 근거 12
- "오늘의 추천" 22/700 → 추천 카드(식사 색 테두리, 이미지 자리 132, 메뉴 22/700, 설명 17, 영양 태그)
- 버튼 2개(이 메뉴로 할게요 / 다른 메뉴 보기) → 32 → AI 재조정 박스(식사 색 40%) → 40 → "오늘 조심할 것"

### 10.3 가사 가이드

- 상단 요약 카드: 가사 색 40% 채움, radius 20, 제목 "오늘의 가사 가이드" 22/700, 안내 15
- 섹션 1·2·3을 각각 `accentWarm` 테두리 카드(radius 20, padding 15×20)로 묶는다
- 섹션 제목: 번호 원 28 + 제목 22/700 + 우측 카운트 12/500(가사 색)
- 공유 카드: info 배경 + 수면 색 테두리 20%, 제목 16/500, 설명 12/500
- 가전 카드: 가사 배경 + "실행" pill 버튼
- 공유 버튼은 `accentWarm` 채움

### 10.4 수면 가이드

- 상단 카드: 수면 색 40% 채움, 제목 22/700, 설명 17, 흰 배경 취침 시간 행(달 아이콘 22 + 시간 18/500)
- "AI가 맞춘 오늘의 수면 환경" 22/700 + 전체 실행 원형 버튼 44(아이콘 24)
- 환경 카드 2열 5장

### 10.5 컨디션 캘린더 (아내·남편 공용)

- 안내 16, 월 제목 26/700, 이전·다음 달 아이콘 버튼 44
- 요일·날짜 셀 44, 날짜 15/400(선택 700), 세로 간격 8
- 컨디션 등급 색: 좋음 `surfaceSubtle` · 보통 primary100 · 나쁨 primary300 · 힘듦 primary500
- 선택 표시는 primary600 테두리 2px
- 범례: 채움 없는 테두리 카드, 제목 13/600, 항목 15
- 선택일 상세: 날짜 24/700 + 주차 15/600, 요약 카드는 `accentWarmBackground` + level1, 항목 간격 26, 라벨 14/600·값 14

### 10.6 Daily 리포트

- Hero: primary600 채움 + borderSubtle, radius 20, 제목 22/700(흰색), 날짜·주차 15(primary50), 구분선 후 안내 12/500(primary50)
- 요약 3칸(실행한 루틴 / 가전 자동 실행 / 가족이 완료한 일): 라벨 12, 값 22/700
- "오늘 실행한 루틴": 분야 배지 42 사각 카드, 건너뛴 항목은 55% 투명, 남편 완료 항목은 info 배지
- 하루 인사이트: 홈캠 주의 카드 + "가족이 함께한 하루"(info 배경)
- 버튼: 저장하고 마치기(primary) / 남편에게 공유(secondary)

### 10.7 남편 초대하기

- 상단 HeroCard
- "연결하면 이런 게 가능해요" 22/700
- 혜택 카드 3장: 번호 원 39(primary600, 숫자 26) + 제목 16/600 + 설명 12
- 버튼 2개 같은 폭(초대하기 / 나중에), 하단 안내 12 좌측 정렬

## 11. AI UI

- AI 결과 카드에는 추천, 이유, 근거, 대안 경로를 함께 둔다.
- 대안 요청 진입(AI 재조정 박스)은 카테고리 색 채움 + 원형 "AI" 아바타로 통일한다.
- 생성 중에는 스켈레톤과 "무엇을 만들고 있는지" 문구를 함께 보여준다.
- 폴백 루틴일 때는 warning 배너로 알리고 재시도 경로를 제공한다.

## 12. Accessibility

- 텍스트 대비 4.5:1, 큰 텍스트(18 이상 또는 14 Bold) 3:1 이상.
- 누를 수 있는 요소는 최소 44×44. 시안이 더 작으면 **보이는 크기는 유지하고 여백으로 터치 영역을 확보**한다(수면 전체 실행 버튼 방식).
- 상태는 색 + 텍스트/아이콘으로 이중 표현한다.
- 아이콘 전용 버튼에는 `tooltip` 또는 `Semantics` 라벨을 둔다.
- 목록·요약 행은 Semantics로 "무엇의 수정인지"까지 읽히게 한다.

> **현재 미해결:** 캘린더의 "나쁨·힘듦" 셀은 진한 글자 3.7:1, 흰 글자 4.15:1로 둘 다 기준 미달이다. 등급 색 조정이 필요하다.

## 13. Motion

- 화면 전환 200~250ms, 카드/배너 등장 150ms, easing은 Material 기본.
- 상태 변화(선택, 완료)는 색과 위치를 동시에 흔들지 않는다.
- 스크롤 중 상단 바는 고정하고 그림자를 만들지 않는다.

## 14. Flutter 구현 규칙

- 색·간격·radius·elevation은 `design_system/tokens`의 토큰을 쓴다. 화면 파일에 hex를 직접 쓰지 않는다. 토큰에 없는 값(15, 11, 26 등 시안 고유값)은 주석으로 출처를 남긴다.
- 공용 컴포넌트를 바꾸면 **모든 사용처를 확인**한다. 한 화면만 바꿔야 하면 옵션 파라미터(`compact`, `color`)로 분기한다.
- 화면 위젯은 데이터를 만들지 않는다. Backend가 주지 않는 값은 FE에서 계산하지 말고 BE 요청으로 남긴다.
- 테스트 key(`home-*`, `meal-*`, `household-*`, `sleep-*`, `calendar-*`, `report-*`)는 디자인 변경 시에도 유지한다.
- 레이아웃 변경으로 테스트가 깨지면 고정 좌표·고정 스크롤 대신 `ensureVisible` 같은 방식으로 테스트를 고친다.

## 15. Anti-patterns

- 모든 요소를 카드로 감싸기
- 한 화면에 Primary 버튼 2개 이상
- 색만으로 상태 구분
- 카테고리 색을 의미 없이 섞어 쓰기
- 시안에 없다는 이유로 Backend 연동 기능(주차 안내 등)을 화면에서 삭제
- 반투명 배경 + 그림자 조합
- 44 미만 터치 영역

## 16. QA Checklist

**토큰·외형**
- [ ] 색·간격·radius가 토큰인가, 예외 값에 주석이 있는가
- [ ] 폰트가 LG EI Headline으로 렌더링되는가(폴백으로 떨어지지 않는가)
- [ ] 카테고리 색이 화면 성격과 맞는가

**레이아웃**
- [ ] 393 / 600 / 1024 / 1440 경계에서 깨지지 않는가
- [ ] 긴 문구에서 줄바꿈·말줄임이 의도대로인가
- [ ] 섹션 간격이 32 기준에서 일관적인가

**상태**
- [ ] Loading·Empty·Error·연동 전 상태가 모두 정의되어 있는가
- [ ] 데이터가 비었을 때 빈 카드만 남지 않는가

**접근성**
- [ ] 대비 4.5:1 이상인가
- [ ] 터치 영역 44 이상인가
- [ ] 아이콘 버튼에 라벨이 있는가

**연동**
- [ ] 화면에 필요한 값이 실제 API 응답에 있는가
- [ ] 없는 값을 FE에서 지어내지 않았는가
- [ ] 테스트 key와 Route가 유지되는가
