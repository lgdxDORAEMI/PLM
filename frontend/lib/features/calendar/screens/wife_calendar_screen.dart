import '../../../routing/route_context.dart';
import 'record_calendar_screen.dart';

class WifeCalendarScreen extends RecordCalendarScreen {
  const WifeCalendarScreen({super.key}) : super(role: AppUserRole.wife);
}
